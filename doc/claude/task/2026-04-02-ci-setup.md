# 2026-04-02 eval-service CI 配置

## 目标

为 eval-service 增加基础 GitHub Actions CI，让每次 push / PR 都先完成最小验证，再进入 merge。

## 本轮范围

1. 建立 `.github/workflows/ci.yml`
2. 运行基础 Python 环境安装
3. 执行后端测试与静态语法检查
4. 保持 workflow 尽量简单，先服务于 merge gate

## 预期检查项

1. 依赖安装可成功
2. 关键测试可运行
3. 基础语法/编译检查可运行
4. 在 PR 与 push 到主分支时自动触发
